import { Meta } from '@storybook/addon-docs'

<Meta title="Context/PrincipalContext/utilities/ThemeInitializer" />

# ThemeInitializer

Actualiza el atributo `data-theme` del documento según el tema actual del `PrincipalContext`.
Se utiliza en `layout.tsx` para mantener el tema al recargar la página.
