import { Meta } from '@storybook/addon-docs'

<Meta title="Context/PrincipalContext/hooks/useAlert" />

# useAlert

Maneja una alerta global dentro de la aplicación.

```ts
const { alert, showAlert, hideAlert } = useAlert();
```

Permite mostrar un objeto `AlertProps` y ocultarlo cuando sea necesario.
