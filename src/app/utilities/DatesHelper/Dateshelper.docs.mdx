import { Meta } from '@storybook/addon-docs'

<Meta title="Utilities/Dateshelper" />

# Dateshelper

Conjunto de funciones para obtener la fecha y hora actual con formato.

- `currentDate()` devuelve `YYYY-MM-DD`
- `currentDateDataBase()` devuelve `YYYY/MM/DD`
- `getTime()` devuelve `HH:MM:SS`
