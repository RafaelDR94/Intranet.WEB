import { Meta } from '@storybook/addon-docs';

<Meta title="MainPage/Accounting/Invoices/ValidateInvoices/useValidateInvoices" />

# useValidateInvoices

Controla la selección y validación de facturas pendientes.

```ts
const {
  handleOpenDetails,
  handleMultiSelectt1,
  handleMultiSelectt2,
  handleMultiValidate,
} = useValidateInvoices();
```
