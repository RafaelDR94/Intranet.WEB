import { Meta } from '@storybook/blocks'

<Meta title="MainPage/Authorizations/AuthorizationDetail/ValesAuthorization/hooks/useValesAuthorization.docs" />

# useValesAuthorization

Hook que gestiona la autorización y el historial de vales de caja chica.

```ts
const {
  rows,
  columns,
  isPendingStatus,
  handleStartApproval,
} = useValesAuthorization()
```
