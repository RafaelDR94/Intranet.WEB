'use client'

/**
 * Alias estable del hook de listado de autorizaciones.
 * Se mantiene para estandarizar el nombre a `useAuthorizationList`.
 */
import useAuthorizationsList from './useAuthorizationsList'

export default useAuthorizationsList
