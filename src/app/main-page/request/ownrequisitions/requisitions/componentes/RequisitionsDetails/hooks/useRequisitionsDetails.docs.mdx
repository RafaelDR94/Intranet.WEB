import { Meta } from '@storybook/blocks'

<Meta title="MainPage/Accounting/PersonalInvoices/Requisitions/RequisitionDetails/hooks/useRequisitionDetails" />

# useRequisitionsDetails

Hook que obtiene los detalles de la requisición actual a partir del parámetro `id` en la URL.

```ts
const { currentRequisition } = useRequisitionsDetails();
```
